<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/colorGrey">

    <ImageView
        android:id="@+id/iv_song_header_play_all"
        style="@style/StyleHeaderImage"
        android:layout_marginStart="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/play" />

    <TextView
        android:id="@+id/tv_song_header_count"
        style="@style/StyleTextView"
        android:layout_marginStart="14dp"
        android:layout_marginTop="0dp"
        android:text="@string/undefined"
        android:textSize="@dimen/sp_14"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/iv_song_header_play_all"
        app:layout_constraintTop_toTopOf="parent" />

    <include layout="@layout/layout_song_list_header" />

</android.support.constraint.ConstraintLayout>